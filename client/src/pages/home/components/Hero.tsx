import { Box, Typography } from '@mui/material';

export const Hero = () => {
	return (
		<section>
			<Box>
				<Typography color={'third.main'} variant='h1'>
					Hero
				</Typography>
			</Box>
			<Box></Box>
		</section>
	);
};
